syntax = "proto3";

enum NotificationKind {
    NOTIFICATION_KIND_ADDED = 0;
    NOTIFICATION_KIND_CHANGED = 1;
    NOTIFICATION_KIND_REMOVED = 2;
}

message AddedChangedRemoved {
    NotificationKind kind = 1;
    repeated int32 fields = 2;
}