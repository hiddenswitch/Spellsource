/*
 * This file is generated by jOOQ.
 */
package com.hiddenswitch.framework.schema.spellsource.tables;


import com.hiddenswitch.framework.schema.keycloak.tables.UserEntity;
import com.hiddenswitch.framework.schema.spellsource.Keys;
import com.hiddenswitch.framework.schema.spellsource.Spellsource;
import com.hiddenswitch.framework.schema.spellsource.enums.RogueRunState;
import com.hiddenswitch.framework.schema.spellsource.tables.records.RogueRunRecord;

import java.time.OffsetDateTime;
import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Function14;
import org.jooq.Identity;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Records;
import org.jooq.Row14;
import org.jooq.Schema;
import org.jooq.SelectField;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * @omit create,update,delete
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class RogueRun extends TableImpl<RogueRunRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>spellsource.rogue_run</code>
     */
    public static final RogueRun ROGUE_RUN = new RogueRun();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<RogueRunRecord> getRecordType() {
        return RogueRunRecord.class;
    }

    /**
     * The column <code>spellsource.rogue_run.id</code>.
     */
    public final TableField<RogueRunRecord, Long> ID = createField(DSL.name("id"), SQLDataType.BIGINT.nullable(false).identity(true), this, "");

    /**
     * The column <code>spellsource.rogue_run.player</code>.
     */
    public final TableField<RogueRunRecord, String> PLAYER = createField(DSL.name("player"), SQLDataType.VARCHAR(36).nullable(false), this, "");

    /**
     * The column <code>spellsource.rogue_run.started_at</code>.
     */
    public final TableField<RogueRunRecord, OffsetDateTime> STARTED_AT = createField(DSL.name("started_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6).nullable(false).defaultValue(DSL.field("now()", SQLDataType.TIMESTAMPWITHTIMEZONE)), this, "");

    /**
     * The column <code>spellsource.rogue_run.ended_at</code>.
     */
    public final TableField<RogueRunRecord, OffsetDateTime> ENDED_AT = createField(DSL.name("ended_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6), this, "");

    /**
     * The column <code>spellsource.rogue_run.hero_class</code>.
     */
    public final TableField<RogueRunRecord, String> HERO_CLASS = createField(DSL.name("hero_class"), SQLDataType.CLOB.nullable(false), this, "");

    /**
     * The column <code>spellsource.rogue_run.deck</code>.
     */
    public final TableField<RogueRunRecord, String> DECK = createField(DSL.name("deck"), SQLDataType.CLOB.nullable(false), this, "");

    /**
     * The column <code>spellsource.rogue_run.bosses_defeated</code>.
     */
    public final TableField<RogueRunRecord, Integer> BOSSES_DEFEATED = createField(DSL.name("bosses_defeated"), SQLDataType.INTEGER.nullable(false).defaultValue(DSL.field("0", SQLDataType.INTEGER)), this, "");

    /**
     * The column <code>spellsource.rogue_run.state</code>.
     */
    public final TableField<RogueRunRecord, RogueRunState> STATE = createField(DSL.name("state"), SQLDataType.VARCHAR.nullable(false).defaultValue(DSL.field("'INITIAL'::spellsource.rogue_run_state", SQLDataType.VARCHAR)).asEnumDataType(com.hiddenswitch.framework.schema.spellsource.enums.RogueRunState.class), this, "");

    /**
     * The column <code>spellsource.rogue_run.game</code>.
     */
    public final TableField<RogueRunRecord, Long> GAME = createField(DSL.name("game"), SQLDataType.BIGINT, this, "");

    /**
     * The column <code>spellsource.rogue_run.opponent_deck</code>.
     */
    public final TableField<RogueRunRecord, String> OPPONENT_DECK = createField(DSL.name("opponent_deck"), SQLDataType.CLOB, this, "");

    /**
     * The column <code>spellsource.rogue_run.seed</code>.
     */
    public final TableField<RogueRunRecord, Long> SEED = createField(DSL.name("seed"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.field("spellsource.generate_seed()", SQLDataType.BIGINT)), this, "");

    /**
     * The column <code>spellsource.rogue_run.seed_state</code>.
     */
    public final TableField<RogueRunRecord, Long> SEED_STATE = createField(DSL.name("seed_state"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.field("0", SQLDataType.BIGINT)), this, "");

    /**
     * The column <code>spellsource.rogue_run.gold</code>.
     */
    public final TableField<RogueRunRecord, Integer> GOLD = createField(DSL.name("gold"), SQLDataType.INTEGER.nullable(false).defaultValue(DSL.field("0", SQLDataType.INTEGER)), this, "");

    /**
     * The column <code>spellsource.rogue_run.lives</code>.
     */
    public final TableField<RogueRunRecord, Integer> LIVES = createField(DSL.name("lives"), SQLDataType.INTEGER.nullable(false).defaultValue(DSL.field("1", SQLDataType.INTEGER)), this, "");

    private RogueRun(Name alias, Table<RogueRunRecord> aliased) {
        this(alias, aliased, null);
    }

    private RogueRun(Name alias, Table<RogueRunRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment("@omit create,update,delete"), TableOptions.table());
    }

    /**
     * Create an aliased <code>spellsource.rogue_run</code> table reference
     */
    public RogueRun(String alias) {
        this(DSL.name(alias), ROGUE_RUN);
    }

    /**
     * Create an aliased <code>spellsource.rogue_run</code> table reference
     */
    public RogueRun(Name alias) {
        this(alias, ROGUE_RUN);
    }

    /**
     * Create a <code>spellsource.rogue_run</code> table reference
     */
    public RogueRun() {
        this(DSL.name("rogue_run"), null);
    }

    public <O extends Record> RogueRun(Table<O> child, ForeignKey<O, RogueRunRecord> key) {
        super(child, key, ROGUE_RUN);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Spellsource.SPELLSOURCE;
    }

    @Override
    public Identity<RogueRunRecord, Long> getIdentity() {
        return (Identity<RogueRunRecord, Long>) super.getIdentity();
    }

    @Override
    public UniqueKey<RogueRunRecord> getPrimaryKey() {
        return Keys.ROGUE_RUN_PKEY;
    }

    @Override
    public List<UniqueKey<RogueRunRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.ROGUE_RUN_DECK_KEY, Keys.ROGUE_RUN_GAME_KEY);
    }

    @Override
    public List<ForeignKey<RogueRunRecord, ?>> getReferences() {
        return Arrays.asList(Keys.ROGUE_RUN__ROGUE_RUN_PLAYER_FKEY, Keys.ROGUE_RUN__ROGUE_RUN_DECK_FKEY, Keys.ROGUE_RUN__ROGUE_RUN_GAME_FKEY, Keys.ROGUE_RUN__ROGUE_RUN_OPPONENT_DECK_FKEY);
    }

    private transient UserEntity _userEntity;
    private transient Decks _rogueRunDeckFkey;
    private transient Games _games;
    private transient Decks _rogueRunOpponentDeckFkey;

    /**
     * Get the implicit join path to the <code>keycloak.user_entity</code>
     * table.
     */
    public UserEntity userEntity() {
        if (_userEntity == null)
            _userEntity = new UserEntity(this, Keys.ROGUE_RUN__ROGUE_RUN_PLAYER_FKEY);

        return _userEntity;
    }

    /**
     * Get the implicit join path to the <code>spellsource.decks</code> table,
     * via the <code>rogue_run_deck_fkey</code> key.
     */
    public Decks rogueRunDeckFkey() {
        if (_rogueRunDeckFkey == null)
            _rogueRunDeckFkey = new Decks(this, Keys.ROGUE_RUN__ROGUE_RUN_DECK_FKEY);

        return _rogueRunDeckFkey;
    }

    /**
     * Get the implicit join path to the <code>spellsource.games</code> table.
     */
    public Games games() {
        if (_games == null)
            _games = new Games(this, Keys.ROGUE_RUN__ROGUE_RUN_GAME_FKEY);

        return _games;
    }

    /**
     * Get the implicit join path to the <code>spellsource.decks</code> table,
     * via the <code>rogue_run_opponent_deck_fkey</code> key.
     */
    public Decks rogueRunOpponentDeckFkey() {
        if (_rogueRunOpponentDeckFkey == null)
            _rogueRunOpponentDeckFkey = new Decks(this, Keys.ROGUE_RUN__ROGUE_RUN_OPPONENT_DECK_FKEY);

        return _rogueRunOpponentDeckFkey;
    }

    @Override
    public RogueRun as(String alias) {
        return new RogueRun(DSL.name(alias), this);
    }

    @Override
    public RogueRun as(Name alias) {
        return new RogueRun(alias, this);
    }

    @Override
    public RogueRun as(Table<?> alias) {
        return new RogueRun(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public RogueRun rename(String name) {
        return new RogueRun(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public RogueRun rename(Name name) {
        return new RogueRun(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public RogueRun rename(Table<?> name) {
        return new RogueRun(name.getQualifiedName(), null);
    }

    // -------------------------------------------------------------------------
    // Row14 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row14<Long, String, OffsetDateTime, OffsetDateTime, String, String, Integer, RogueRunState, Long, String, Long, Long, Integer, Integer> fieldsRow() {
        return (Row14) super.fieldsRow();
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Function)}.
     */
    public <U> SelectField<U> mapping(Function14<? super Long, ? super String, ? super OffsetDateTime, ? super OffsetDateTime, ? super String, ? super String, ? super Integer, ? super RogueRunState, ? super Long, ? super String, ? super Long, ? super Long, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(Records.mapping(from));
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Class,
     * Function)}.
     */
    public <U> SelectField<U> mapping(Class<U> toType, Function14<? super Long, ? super String, ? super OffsetDateTime, ? super OffsetDateTime, ? super String, ? super String, ? super Integer, ? super RogueRunState, ? super Long, ? super String, ? super Long, ? super Long, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(toType, Records.mapping(from));
    }
}
